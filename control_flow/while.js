// for loop
for (let index = 0; index < 5; index++) {
    if (index % 2 !== 0)
    console.log(index);
}

// as a while loop
let index = 0;
while (index < 5) {
    if (index % 2 !== 0)
        console.log(index);
    index++;
}