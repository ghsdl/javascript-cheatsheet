// if a customer has more than 100 points
// they are a gold customer otherwise
// they are a silver customer

let points = 90;
// let points = 110;
let type = points > 100 ? 'gold ' : 'silver';
// if the condition points > 100
// is true ? 'gold ' it's a gold customer 
// otherwise it's fault : 'silver' and it's a silver customer

console.log(type);